<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected: brand fill. Unselected: the same contrast surface every other
         item on a white screen uses — never a bare outline. -->
    <item android:color="@color/appColor" android:state_checked="true" />
    <item android:color="@color/surface_1" />
</selector>
