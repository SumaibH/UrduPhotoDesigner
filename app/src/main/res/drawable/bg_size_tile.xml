<?xml version="1.0" encoding="utf-8"?>
<!-- Size tile: a white card on the white page, held together by its stroke. The
     artboard FramePreviewView draws inside it is the contrast step. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/surface_2" />
    <stroke
        android:width="1dp"
        android:color="@color/surface_stroke" />
    <corners android:radius="10dp" />
</shape>
