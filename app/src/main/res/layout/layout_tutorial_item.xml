<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingBottom="20dp">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/thumbCard"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:cardBackgroundColor="@color/surface_1"
        app:cardCornerRadius="14dp"
        app:cardElevation="0dp"
        app:layout_constraintDimensionRatio="H,16:9"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <com.facebook.shimmer.ShimmerFrameLayout
            android:id="@+id/shimmerLayout"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:shimmer_auto_start="true"
            app:shimmer_base_alpha="0.5"
            app:shimmer_base_color="@color/light_gray"
            app:shimmer_colored="true"
            app:shimmer_duration="700"
            app:shimmer_highlight_alpha="@dimen/shimmer_highlight_alpha"
            app:shimmer_repeat_delay="0">

            <ImageView
                android:id="@+id/thumbnail"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/surface_1"
                android:contentDescription="@null"
                android:scaleType="centerCrop" />

        </com.facebook.shimmer.ShimmerFrameLayout>

    </com.google.android.material.card.MaterialCardView>

    <!-- Sits above the shimmer so it does not pulse with the placeholder. -->
    <ImageView
        android:id="@+id/playBadge"
        android:layout_width="52dp"
        android:layout_height="37dp"
        android:contentDescription="@null"
        android:src="@drawable/ic_youtube_play_badge"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/thumbCard"
        app:layout_constraintEnd_toEndOf="@id/thumbCard"
        app:layout_constraintStart_toStartOf="@id/thumbCard"
        app:layout_constraintTop_toTopOf="@id/thumbCard"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/duration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        android:background="@drawable/bg_tutorial_meta"
        android:fontFamily="@font/medium"
        android:paddingHorizontal="7dp"
        android:paddingVertical="3dp"
        android:textColor="@color/white"
        android:textSize="11sp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="@id/thumbCard"
        app:layout_constraintEnd_toEndOf="@id/thumbCard"
        tools:text="1.2K views"
        tools:visibility="visible" />

    <TextView
        android:id="@+id/videoTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:ellipsize="end"
        android:fontFamily="@font/medium"
        android:maxLines="2"
        android:textColor="@color/black"
        android:textSize="14sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/thumbCard"
        tools:text="How to Design Urdu Logo in Mobile in 2 Minutes" />

    <TextView
        android:id="@+id/meta"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="3dp"
        android:ellipsize="end"
        android:fontFamily="@font/regular"
        android:maxLines="1"
        android:textColor="@color/gray"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/videoTitle"
        tools:text="1.2K views · 2 weeks ago" />

</androidx.constraintlayout.widget.ConstraintLayout>
